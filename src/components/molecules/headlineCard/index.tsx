import React, { FC } from "react";
import { StyleSheet } from "react-native";

interface IHeadlineCard {}

export const HeadlineCard: FC<IHeadlineCard> = ({}): React.ReactElement => {
  return <></>;
};

const styles = StyleSheet.create({});
